# Resource definition for Create operations
allOf:
  - $ref: './InformationStandard.yaml'
    # Extend
  - type: object
    properties:
      mainVersionId:
        writeOnly: true
      mainVersion:
        readOnly: false
        writeOnly: true
  - $ref: '../../_schemas/BasicResponse.yaml'