# Resource definition for Create operations
allOf:
  - $ref: './Hl7Standard.yaml'
    # Extend
  - type: object
    properties:
      mainVersionId:
        readOnly: true
        writeOnly: true
      mainVersion:
        writeOnly: true
  - $ref: '../../_schemas/BasicResponse.yaml'