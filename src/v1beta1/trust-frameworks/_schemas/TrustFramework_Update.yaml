# Resource definition for Update operations
allOf:
  - $ref: './TrustFramework.yaml'
    # Extend
  - type: object
    properties:
      organizationId:
        readOnly: true
      mainVersionId:
        description: The ID of the main version of the trust framework
        type:
          - 'string'
          - 'null'
        format: uuid
        examples:
          - 78ca8495-a4f4-4b41-b97b-c912c2e96450