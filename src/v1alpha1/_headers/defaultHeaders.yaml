# Convenience file with default headers for an operation, which can be used to easily add default header definitions to
# a response definition like so:
# 
# headers:
#   $ref: './defaultHeaders.yaml'